name: Keep Alive

on:
  schedule:
    # Run every 14 minutes to prevent Render free tier sleep (15m limit)
    - cron: '*/14 * * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Server
        run: |
          # Replace with your actual Render URL
          curl -I https://realestateleadai.onrender.com/api/health
